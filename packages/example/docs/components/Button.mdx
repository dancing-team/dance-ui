---
sidebar_position: 1
---

import ComponentSource from '!!raw-loader!../../../components/src/Button/Button'
import { Button, Space } from '@dance-ui/ui'

# Button 按钮

按钮组件

import DemoSrc from '!!raw-loader!../../../components/src/Button/demo'
import Demo from '../../../components/src/Button/demo'

<DemoBlock title="基本使用" src={DemoSrc}>
  <Demo />
</DemoBlock>

## 演示

### type

默认为 `default` 按钮

`default` 表示默认按钮，`primary` 表示主要按钮，`link` 表示无边框按钮， `unstyle` 表示不带任何样式的按钮（方便自己定制）

## 组件源码

<CodeBlock title="组件源码" language={'tsx'} showLineNumbers>
  {ComponentSource}
</CodeBlock>
